#registry
  %h1
    REGISTRY
  %p
    We are registered at the following venues
  .section
    =link_to image_tag("#{@asset_root}/images/registry/crate.jpg", :class=>'center'), @amazon_registry_url
    %br
    =link_to("Crate & Barrel", @crate_registry_url, :class=>'title purple')